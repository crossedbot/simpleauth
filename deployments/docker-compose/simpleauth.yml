services:
    mongo:
        image: mongo:latest
    simpleauth:
        image: crossedbot/simpleauth:latest
        depends_on:
            - mongo
        links:
            - mongo
        ports:
            - "${PORT}:8080"
